import {
  OrbitControls
} from "./chunk-RXF3OCVG.js";
import {
  handConfig
} from "./chunk-X6ZJW5JI.js";
import {
  ACESFilmicToneMapping,
  AmbientLight,
  BoxGeometry,
  Color,
  CylinderGeometry,
  DirectionalLight,
  DoubleSide,
  Group,
  HemisphereLight,
  Mesh,
  MeshStandardMaterial,
  PCFSoftShadowMap,
  PerspectiveCamera,
  PlaneGeometry,
  PointLight,
  Scene,
  Vector3,
  WebGLRenderer,
  setSceneObjects
} from "./chunk-YUBYZ5OR.js";
import "./chunk-G3PMV62Z.js";

// node_modules/handible/tableSetup.js
function setupTableScene() {
  let scene, camera, renderer, controls;
  scene = new Scene();
  scene.background = new Color(8900331);
  const floorGeometry = new PlaneGeometry(10, 10);
  const floorMaterial = new MeshStandardMaterial({
    color: 14540253,
    roughness: 0.8,
    metalness: 0.2
  });
  const floor = new Mesh(floorGeometry, floorMaterial);
  floor.rotation.x = -Math.PI / 2;
  floor.position.y = -2.5;
  floor.receiveShadow = true;
  scene.add(floor);
  const tableGeometry = new BoxGeometry(3, 0.2, 2);
  const tableMaterial = new MeshStandardMaterial({
    color: 9127187,
    roughness: 0.5,
    metalness: 0.1
  });
  const tableTop = new Mesh(tableGeometry, tableMaterial);
  tableTop.position.set(0, -1.3, -1);
  tableTop.castShadow = true;
  tableTop.receiveShadow = true;
  tableTop.userData.isTable = true;
  scene.add(tableTop);
  const legGeometry = new CylinderGeometry(0.1, 0.1, 1.2, 32);
  const legMaterial = new MeshStandardMaterial({ color: 9127187 });
  const positions = [
    { x: 1.4, z: 0.9 },
    { x: 1.4, z: -0.9 },
    { x: -1.4, z: 0.9 },
    { x: -1.4, z: -0.9 }
  ];
  positions.forEach((pos) => {
    const leg = new Mesh(legGeometry, legMaterial);
    leg.position.set(pos.x, -1.9, pos.z - 1);
    leg.castShadow = true;
    scene.add(leg);
  });
  camera = new PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1e3);
  camera.position.set(0, 1.3, 0.8);
  const canvas = document.getElementById("threeCanvas");
  renderer = new WebGLRenderer({ canvas, antialias: true });
  renderer.setSize(window.innerWidth, window.innerHeight);
  renderer.setPixelRatio(window.devicePixelRatio);
  renderer.shadowMap.enabled = true;
  renderer.shadowMap.type = PCFSoftShadowMap;
  renderer.toneMapping = ACESFilmicToneMapping;
  renderer.toneMappingExposure = 1.2;
  controls = new OrbitControls(camera, renderer.domElement);
  controls.enableDamping = true;
  controls.dampingFactor = 0.05;
  controls.enableZoom = true;
  controls.minDistance = 1;
  controls.maxDistance = 10;
  controls.target.set(0, 0, -1);
  const ambientLight = new AmbientLight(16777215, 0.4);
  scene.add(ambientLight);
  const hemiLight = new HemisphereLight(16777215, 4473924, 0.6);
  hemiLight.position.set(0, 20, 0);
  scene.add(hemiLight);
  const sunLight = new DirectionalLight(16777215, 1.2);
  sunLight.position.set(5, 10, 5);
  sunLight.castShadow = true;
  sunLight.shadow.mapSize.width = 2048;
  sunLight.shadow.mapSize.height = 2048;
  sunLight.shadow.camera.near = 0.5;
  sunLight.shadow.camera.far = 50;
  sunLight.shadow.camera.left = -10;
  sunLight.shadow.camera.right = 10;
  sunLight.shadow.camera.top = 10;
  sunLight.shadow.camera.bottom = -10;
  scene.add(sunLight);
  const pointLight = new PointLight(16777215, 0.8, 10);
  pointLight.position.set(0, 2, 0);
  pointLight.castShadow = true;
  scene.add(pointLight);
  const chessboardGroup = new Group();
  chessboardGroup.isChessboard = true;
  const squareSize = 0.2;
  const chessboardSize = squareSize * 8;
  const halfSize = chessboardSize / 2;
  for (let row = 0; row < 8; row++) {
    for (let col = 0; col < 8; col++) {
      const color = (row + col) % 2 === 0 ? 16777215 : 0;
      const squareGeometry = new PlaneGeometry(squareSize, squareSize);
      const squareMaterial = new MeshStandardMaterial({ color, side: DoubleSide });
      const square = new Mesh(squareGeometry, squareMaterial);
      square.position.set(
        col * squareSize - halfSize + squareSize / 2,
        // Center x
        0.101,
        // Slightly above table top (thickness 0.2 / 2 = 0.1 + epsilon)
        row * squareSize - halfSize + squareSize / 2
        // Center z
      );
      square.rotation.x = -Math.PI / 2;
      square.userData.defaultColor = color;
      chessboardGroup.add(square);
    }
  }
  chessboardGroup.position.set(0, 0, 0);
  tableTop.add(chessboardGroup);
  const piecePositions = [
    { row: 0, col: 0, color: 16711680 },
    // Red piece on (0,0)
    { row: 7, col: 7, color: 16711680 }
    // Red piece on (7,7)
  ];
  piecePositions.forEach((pos) => {
    const squareIndex = pos.row * 8 + pos.col;
    const targetSquare = chessboardGroup.children[squareIndex];
    if (targetSquare) {
      const pieceGeometry = new CylinderGeometry(0.08, 0.08, 0.15, 32);
      const pieceMaterial = new MeshStandardMaterial({ color: pos.color });
      const piece = new Mesh(pieceGeometry, pieceMaterial);
      piece.position.copy(targetSquare.position.clone().add(new Vector3(0, 0.08, 0)));
      piece.castShadow = true;
      piece.userData.isGrabbable = true;
      chessboardGroup.add(piece);
    }
  });
  handConfig.xScale = 2;
  handConfig.yScale = -2;
  handConfig.zMagnification = 2;
  handConfig.zOffset = 0;
  handConfig.rotationOffset.set(-Math.PI / 12, 0, 0);
  setSceneObjects({ scene, camera, renderer, controls });
}
export {
  setupTableScene
};
//# sourceMappingURL=tableSetup-DLH6PQIP.js.map
