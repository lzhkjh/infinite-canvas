import {
  AudioSystem,
  BUTTON_HOVER_THRESHOLD,
  CHESSBOARD_SIZE,
  HIGHLIGHT_COLOR,
  UIBUTTON_HOVER_THRESHOLD,
  UI_CURSOR_THRESHOLD,
  audioSystem,
  cleanupHandTracking,
  getConeVisualsPerHand,
  getForwardDirection,
  getHandTrackingData,
  getRayVisualsPerHand,
  getWristPosition,
  grabNearestObject,
  handConfig,
  initGestureControl,
  isPinching2D,
  isPinchingState,
  lastSnappedSquarePerHand,
  onPinchEnd,
  onPinchStart,
  predictWebcam,
  registerOnPinchEnd,
  registerOnPinchStart,
  sceneLoader,
  setButtonVolume,
  setupHandTracking,
  surfaceSystem,
  toggleButtonSounds,
  updateRaycast
} from "./chunk-UXGOO7OD.js";
import {
  getSceneObjects,
  setSceneObjects
} from "./chunk-YUBYZ5OR.js";
import "./chunk-BUSYA2B4.js";

// node_modules/handible/mediaPipeSetup.js
import { FilesetResolver, HandLandmarker } from "https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@latest";
async function activateWebcamInCorner(videoElement) {
  try {
    const stream = await navigator.mediaDevices.getUserMedia({ video: true });
    videoElement.srcObject = stream;
    videoElement.onloadeddata = () => {
      console.log("웹캠 비디오 로드 완료.");
    };
    videoElement.style.position = "fixed";
    videoElement.style.bottom = "20px";
    videoElement.style.right = "20px";
    videoElement.style.width = "240px";
    videoElement.style.height = "180px";
    videoElement.style.borderRadius = "12px";
    videoElement.style.border = "2px solid #00ffff";
    videoElement.style.boxShadow = "0 4px 8px rgba(0, 0, 0, 0.4)";
    videoElement.style.zIndex = "100";
    videoElement.style.transform = "scaleX(-1)";
    document.body.appendChild(videoElement);
  } catch (error) {
    console.error("웹캠 접근 오류:", error);
    document.getElementById("loading-message").textContent = "오류: 웹캠에 접근할 수 없습니다. 카메라 접근을 허용해 주십시오.";
    document.getElementById("loading-message").style.color = "red";
  }
}
async function initializeMediaPipe(videoElement) {
  console.log("MediaPipe HandLandmarker 모델 로드 중...");
  const vision = await FilesetResolver.forVisionTasks(
    "https://cdn.jsdelivr.net/npm/@mediapipe/tasks-vision@latest/wasm"
  );
  const handLandmarker = await HandLandmarker.createFromOptions(vision, {
    baseOptions: {
      modelAssetPath: `https://storage.googleapis.com/mediapipe-models/hand_landmarker/hand_landmarker/float16/1/hand_landmarker.task`,
      delegate: "GPU"
    },
    runningMode: "VIDEO",
    numHands: 2
  });
  console.log("MediaPipe HandLandmarker 로드 완료.");
  await activateWebcamInCorner(videoElement);
  videoElement.onloadeddata = () => predictWebcam(videoElement, handLandmarker);
  return handLandmarker;
}

// node_modules/handible/index.js
async function startGestureControl(videoElement, scene, numHands = 2) {
  const handLandmarker = await initializeMediaPipe(videoElement);
  await setupHandTracking(scene);
  initGestureControl(scene, numHands);
  return handLandmarker;
}
export {
  AudioSystem,
  BUTTON_HOVER_THRESHOLD,
  CHESSBOARD_SIZE,
  HIGHLIGHT_COLOR,
  surfaceSystem as SurfaceInteractionSystem,
  UIBUTTON_HOVER_THRESHOLD,
  UI_CURSOR_THRESHOLD,
  activateWebcamInCorner,
  audioSystem,
  cleanupHandTracking,
  getConeVisualsPerHand,
  getForwardDirection,
  getHandTrackingData,
  getRayVisualsPerHand,
  getSceneObjects,
  getWristPosition,
  grabNearestObject,
  handConfig,
  initGestureControl,
  initializeMediaPipe,
  isPinching2D,
  isPinchingState,
  lastSnappedSquarePerHand,
  onPinchEnd,
  onPinchStart,
  predictWebcam,
  registerOnPinchEnd,
  registerOnPinchStart,
  sceneLoader,
  setButtonVolume,
  setSceneObjects,
  setupHandTracking,
  startGestureControl,
  toggleButtonSounds,
  updateRaycast
};
//# sourceMappingURL=handible.js.map
